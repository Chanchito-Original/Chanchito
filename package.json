{
    "name": "chanchito",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm --prefix src/client run start\" \"npm --prefix src/server run server-dev\""
    },
    "dependencies": {
        "cors": "^2.8.5",
        "pg": "^8.11.3"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
