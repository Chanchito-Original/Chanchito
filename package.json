{
    "name": "chanchito",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "start": "npm --prefix src/client run start",
      "build": "concurrently \"npm --prefix src/client run build\" \"npm --prefix src/server run build\"",
      "dev": "npm --prefix src/client run start"
    },
    "dependencies": {
      "@vercel/postgres": "^0.5.1",
      "bootstrap": "^5.3.2",
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "pg": "^8.11.3",
      "react-scripts": "^5.0.1"
    },
    "devDependencies": {
      "concurrently": "^8.2.2"
    }
  }
  